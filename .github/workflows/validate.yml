name: Validate plugin

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Validate bundle
        run: python3 tests/validate_bundle.py
      - name: Run tests
        run: python3 -m unittest discover -s tests -p 'test_*.py'
