interface:
  display_name: "Nuko Nova Unslop"
  short_description: "Default anti-slop standard for human-facing text"
  default_prompt: "Use $nuko-nova-unslop as the default editorial standard for every human-facing response and prose artifact."

policy:
  allow_implicit_invocation: true
