interface:
  display_name: "Nuko Nova Unslop"
  short_description: "Human writing with no slop or cringe"
  default_prompt: "Use $nuko-nova-unslop as the human-writing standard for every human-facing response and prose artifact."

policy:
  allow_implicit_invocation: true
