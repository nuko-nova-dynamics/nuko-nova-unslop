# Security

Report security concerns privately to `hello@nukonova.com`.

The runtime skill and bundled checks run locally and require no credentials or network calls. The optional upstream-review helper contacts GitHub only when a maintainer explicitly runs it with remote refresh enabled. Do not place confidential drafts in public test fixtures, issue bodies, workflow artifacts, or upstream-review reports.
